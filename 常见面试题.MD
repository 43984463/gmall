##数据库索引失效常见场景
1、在索引列上使用函数。如SUBSTR,DECODE,INSTR等，对索引列进行运算.需要建立函数索引就可以解决了。

2、应尽量避免在 where 子句中使用 != 或 <> 操作符。

3、尽量避免在 where 子句中使用 or 来连接条件。

3、like "%_" 百分号在前。

4、单独引用复合索引里非第一位置的索引列(索引的最左匹配原则)。

5、字符型字段为数字时在where条件里不添加引号(如果列类型是字符串，那一定要在条件中将数据使用引号引用起来，否则不会使用索引)。

6、B-tree索引 is null不会走,is not null会走,位图索引 is null,is not null  都会走、联合索引 is not null 只要在建立的索引列（不分先后）都会走。

7、应尽量避免在 where 子句中对字段进行表达式操作。(select id from t where num/2 = 100 改为 select id from t where num = 100*2；)

##接口幂等性处理
前端页面：
在提交之后禁用提交按钮，直到接口返回结果。

后端接口：
1、使用数据库的锁(悲观锁或者乐观锁)
插入的话进行唯一性的验证(使用数据库保持幂等)，更新的话进行版本的判断(乐观锁)。</br>
2、使用token机制
 token机制实现步骤:

 1. 生成全局唯一的token,token放到redis(建议放在redis)或jvm内存,token会在页面跳转时获取.存放到pageScope中,支付请求提交先获取token

 2. 提交后后台校验token，执行提交逻辑,提交成功同时删除token，如果执行失败重新请求生成token的接口并生成新的token更新redis ,这样当第一次提交后token更新了,页面再次提交携带的token是已删除的token后台验证会失败不让提交


 token特点：   要申请，一次有效性，可以限流
(使用 redis的lua脚本进行删除，成功说明可以继续执行代码，失败则直接返回)
注意： redis要用删除操作来判断token，删除成功代表token校验通过，如果用select+delete来校验token，存在并发问题，不建议使用

3、数据库的唯一索引保持插入的幂等
4、分布式锁

##spring事务不回滚
1、数据库是否支持事务(mysql的innoDb支持事务，但是MyIsam不支持事务)
2、标注的方法是否是public(private,默认和protect不行, final不行，static不行)
3、异常被捕捉并且没有手动抛出spring无法感知就不能回滚。
4、同一个class中的2个方法进行调用不能回滚(没有经过动态代理，spring不能感知)
5、调用的该方法的类不是被spring管理的类