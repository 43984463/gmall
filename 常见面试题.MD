## 数据库索引失效常见场景
1、在索引列上使用函数。如SUBSTR,DECODE,INSTR等，对索引列进行运算.需要建立函数索引就可以解决了。

2、应尽量避免在 where 子句中使用 != 或 <> 操作符。

3、尽量避免在 where 子句中使用 or 来连接条件。

3、like "%_" 百分号在前。

4、单独引用复合索引里非第一位置的索引列(索引的最左匹配原则)。

5、字符型字段为数字时在where条件里不添加引号(如果列类型是字符串，那一定要在条件中将数据使用引号引用起来，否则不会使用索引)。

6、B-tree索引 is null不会走,is not null会走,位图索引 is null,is not null  都会走、联合索引 is not null 只要在建立的索引列（不分先后）都会走。

7、应尽量避免在 where 子句中对字段进行表达式操作。(select id from t where num/2 = 100 改为 select id from t where num = 100*2；)

## 接口幂等性处理
前端页面：
在提交之后禁用提交按钮，直到接口返回结果。

后端接口：
1、使用数据库的锁(悲观锁或者乐观锁)
插入的话进行唯一性的验证(使用数据库保持幂等)，更新的话进行版本的判断(乐观锁)。</br>
2、使用token机制
 token机制实现步骤:

 1. 生成全局唯一的token,token放到redis(建议放在redis)或jvm内存,token会在页面跳转时获取.存放到pageScope中,支付请求提交先获取token

 2. 提交后后台校验token，执行提交逻辑,提交成功同时删除token，如果执行失败重新请求生成token的接口并生成新的token更新redis ,这样当第一次提交后token更新了,页面再次提交携带的token是已删除的token后台验证会失败不让提交


 token特点：   要申请，一次有效性，可以限流
(使用 redis的lua脚本进行删除，成功说明可以继续执行代码，失败则直接返回)
注意： redis要用删除操作来判断token，删除成功代表token校验通过，如果用select+delete来校验token，存在并发问题，不建议使用

3、数据库的唯一索引保持插入的幂等
4、分布式锁

## spring事务不回滚
1、数据库是否支持事务(mysql的innoDb支持事务，但是MyIsam不支持事务)
2、标注的方法是否是public(private,默认和protect不行, final不行，static不行)
3、异常被捕捉并且没有手动抛出spring无法感知就不能回滚。
4、同一个class中的2个方法进行调用不能回滚(没有经过动态代理，spring不能感知)
5、调用的该方法的类不是被spring管理的类

## jvm
1、JVM主要组成部分及其作用。
![Image text](https://github.com/43984463/gmall/blob/master/JVM.png)
JVM包含两个子系统和两个组件，两个子系统为Class loader(类装载)、Execution engine(执行引擎)；两个组件为Runtime data area(运行时数据区)、Native Interface(本地接口)。

* Class loader(类装载)：根据给定的全限定名类名(如：java.lang.Object)来装载class文件到Runtime data area中的method area。

* Execution engine（执行引擎）：执行classes中的指令。

* Native Interface(本地接口)：与native libraries交互，是其它编程语言交互的接口。

* Runtime data area(运行时数据区域)：这就是我们常说的JVM的内存。

> 作用 ：首先通过编译器把 Java 代码转换成字节码，类加载器（ClassLoader）再把字节码加载到内存中，将其放在运行时数据区（Runtime data area）的方法区内，而字节码文件只是 JVM 的一套指令集规范，并不能直接交给底层操作系统去执行，因此需要特定的命令解析器执行引擎（Execution Engine），将字节码翻译成底层系统指令，再交由 CPU 去执行，而这个过程中需要调用其他语言的本地库接口（Native Interface）来实现整个程序的功能。

### 下面是Java程序运行机制详细说明

**Java程序运行机制步骤**

> 首先利用IDE集成开发工具编写Java源代码，源文件的后缀为.java；
再利用编译器(javac命令)将源代码编译成字节码文件，字节码文件的后缀名为.class；
运行字节码的工作是由解释器(java命令)来完成的。

## JVM 运行时数据区
#### Java 虚拟机在执行 Java 程序的过程中会把它所管理的内存区域划分为若干个不同的数据区域。这些区域都有各自的用途，以及创建和销毁的时间，有些区域随着虚拟机进程的启动而存在，有些区域则是依赖线程的启动和结束而建立和销毁。Java 虚拟机所管理的内存被划分为如下几个区域：
1. 程序计数器（Program Counter Register）：当前线程所执行的字节码的行号指示器，字节码解析器的工作是通过改变这个计数器的值，来选取下一条需要执行的字节码指令，分支、循环、跳转、异常处理、线程恢复等基础功能，都需要依赖这个计数器来完成；
2. Java 虚拟机栈（Java Virtual Machine Stacks）：用于存储局部变量表(各个方法中的局部变量,如果是引用类型，存储的是这个对象在堆中的地址)、操作数栈(java虚拟机栈中的一个用于计算的临时数据存储区,  操作数栈主要用于保存计算过程的中间结果，同时作为计算过程中变量临时存储空间。)、动态链接、方法出口等信息；(main最先进栈，先进后出)
3. 本地方法栈（Native Method Stack）：与虚拟机栈的作用是一样的，只不过虚拟机栈是服务 Java 方法的，而本地方法栈是为虚拟机调用 Native 方法服务的;
******************************************************************************************************************************************************************************************************************************************************************************************
4. Java 堆（Java Heap）：Java 虚拟机中内存最大的一块，是被所有线程共享的，几乎所有的对象实例都在这里分配内存；
5. 方法区（Methed Area）(HotSpot 在JAVA7的时候方法区的实现是永久代，JAVA8之后称为元空间)：用于存储已被虚拟机加载的类信息、常量、静态变量、即时编译后的代码等数据(JAVA8使用的是直接内存，非JVM内存，而是电脑系统的直接内存)。


```text
要看springboot还有springcloud  只要是和spring相关的源代码  一定要先对spring的源码很熟悉

openfeign的远程调用原理 (为啥写一个接口就能调用远程的服务)
nacos作为配置中心如何自动刷新
注册中心ZooKeeper、Eureka、Consul 、Nacos对比
集合底层的代码( List Set Map 实现和源码原理)  深点就是数据结构
线程池的实际使用例子
多线程在实际开发中的使用
springboot自动配置原理
注册中心是如何感知微服务的上线和下线的， 又是如何调用远程服务的
微服务在linux环境的配置和启动
在linux环境查看正在运行的服务
sql的优化 (索引   分库分表  主从复制)
redis的主从复制  哨兵模式  redis经常作为什么使用(除了缓存数据)
linux常用命令
rabbitMQ的常见工作模式  保证消息的可靠送达和不重复消费
springMVC的调用流程
CSRF攻击 (暂时面试没问到)
接口幂等性 
怎么防止刷接口(大量请求直接访问某个接口  直接在接口第一步就进行一些参数校验(例如验证手机号 + 验证码 错误直接返回 具体网上再查))
JVM的模型和常用参数调整
分布式锁的原理
设计模式以及设计模式的实际使用(感觉挺重要好多面试都会问)
mybatis因为我说项目中一直没用到  所以没问
tomcat源码
java8新特性以及使用
实现生产者和消费者的方式(wait/notify， 使用Lock中Condition的await/signalAll实现生产者-消费者   使用BlockingQueue实现生产者-消费者)https://thinkwon.blog.csdn.net/article/details/102557126
String 得多刷关于String的面试题
JVM模型和JMM模型 (volatile)
ClassLoader类加载机制和类加载器
https://www.bilibili.com/video/BV1xv411C7ub 和  https://www.bilibili.com/video/BV1ja4y1j7RF  这个视频里面讲的常见的问题   不过广告有点多
关于网络  HTTP 方面的知识
自己做的项目的深度理解 总结
```
